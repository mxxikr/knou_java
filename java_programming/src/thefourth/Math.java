package thefourth;

public class Math {
    public static double sqrt(double a) {
//        this.a = a; // static 메소드 안에서 this 사용 불가능
        Math.sqrt(2.0); // 정적 메소드 호출 예 
        Integer.parseInt("102");
        
        return Math.sqrt(a);
    }
}
